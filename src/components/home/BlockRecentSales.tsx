import { FC } from "react"
import styled from "styled-components";
import { Box, Text } from "@chakra-ui/react"

// Show last 5 NFTs
// Include owner
// Include price
// Include name
// Include image
// paginate to next 5(start with tradedVolume())

const BlockRecentSales: FC = () => {
  return (
    <Box>
      <Text fontSize="lg" fontWeight="bold">View recent sales here...</Text>
    </Box>
  )
}

export default BlockRecentSales;
